# Template for local secrets. Copy to .env and fill in real values.
# .env is gitignored; .env.example is committed as documentation.

# Official IMD API key (api.imd.gov.in)
# Get one from IMD; used by scripts/fetch_imd_districtwarning.py
IMD_API_KEY=YOUR_KEY_HERE
